
from user import views
from django.urls import path

urlpatterns = [
    # path('login/', views.sign_up, 'login'),
    # path('register/', views.register, 'register'),
    # path('logout/', views.logout, 'logout')
]